name: ch12-health-exercise

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.starter
    ports:
      - "127.0.0.1:9001:9000"
    healthcheck:
      test: ["CMD", "true"]
      interval: 5s
      timeout: 3s
      retries: 2
      start_period: 5s
