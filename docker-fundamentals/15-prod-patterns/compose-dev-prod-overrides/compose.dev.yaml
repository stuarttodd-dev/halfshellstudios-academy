services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/var/www/html
    environment:
      APP_DEBUG: "true"
      LOG_LEVEL: debug

  web:
    volumes:
      - ./html:/usr/share/nginx/html:ro
