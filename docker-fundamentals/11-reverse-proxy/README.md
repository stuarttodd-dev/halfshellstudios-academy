# Chapter 11 — Reverse proxy

**Course chapter:** [Reverse proxy](https://docker.learnio.dev/learn/sections/chapter-reverse-proxy/)

Example code for chapter 11 lessons.

| Lesson | Folder |
| ------ | ------ |
| nginx in front of PHP-FPM in Compose | [nginx-fpm-socket](nginx-fpm-socket/) |
| FastCGI sockets vs TCP ports | [nginx-fpm-socket](nginx-fpm-socket/) (`demo-fastcgi.sh`, `compose.socket.yaml`) |
| curl smoke through the proxy | [nginx-fpm-socket](nginx-fpm-socket/) (`demo-curl-smoke.sh`) |
| Security headers and hide server tokens | [nginx-fpm-socket](nginx-fpm-socket/) (`demo-security-headers.sh`) |
| Exercise / Solution: nginx plus PHP-FPM | [exercise-nginx-plus-php-fpm](exercise-nginx-plus-php-fpm/) |
| The proxy owns TLS; plain FastCGI inside | [tls-edge-plain-fastcgi](tls-edge-plain-fastcgi/) |

← [Docker fundamentals](../README.md)
