# Deliberate mistake — no chown before USER www-data (lesson step 2)
FROM php:8.3-fpm-bookworm

WORKDIR /var/www/html

COPY . .

RUN mkdir -p storage/logs bootstrap/cache

USER www-data

EXPOSE 9000
CMD ["php-fpm"]
