# Fragile: numeric stage index shifts when you insert a new FROM above.
FROM alpine:3.20
RUN apk add --no-cache build-base \
 && mkdir -p /out \
 && echo "v1 artifact" > /out/release.txt

FROM alpine:3.20
COPY --from=0 /out/release.txt /release.txt
CMD ["cat", "/release.txt"]
