# Lesson 7.7 — operator toolkit: config, ps, top, logs, run
name: ch7stack

services:
  web:
    image: nginx:stable-alpine
    ports:
      - "127.0.0.1:${HTTP_PORT:-8080}:80"
    volumes:
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - php

  php:
    image: ${PHP_IMAGE:-ch7stack-php:local}
    environment:
      APP_ENV: local
      DB_HOST: db
    depends_on:
      - db

  db:
    image: mariadb:11
    environment:
      MARIADB_DATABASE: app
      MARIADB_USER: app
      MARIADB_PASSWORD: ${DB_PASSWORD:-devpass}
    volumes:
      - dbdata:/var/lib/mysql

volumes:
  dbdata:
