# Dev merge — bind mount + debug env (lesson 7.6 pattern)
services:
  php:
    build: .
    image: ch7stack-php:local
    volumes:
      - .:/var/www/html
    environment:
      APP_DEBUG: "true"
      LOG_LEVEL: debug

  web:
    volumes:
      - .:/var/www/html:ro
