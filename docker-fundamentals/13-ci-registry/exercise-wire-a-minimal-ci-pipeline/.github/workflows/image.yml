name: CI image pipeline

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  workflow_dispatch:

# Task 1: permissions (contents read, packages write, pull-requests write)

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          # Task 2: username and password for GITHUB_TOKEN

      - name: Build for smoke and scan
        uses: docker/build-push-action@v6
        with:
          context: .
          push: false
          load: true
          tags: ci-candidate:${{ github.sha }}

      - name: Smoke test
        run: |
          # Task 3: docker run smoke.php output

      - name: Trivy scan
        uses: aquasecurity/trivy-action@0.28.0
        with:
          image-ref: ci-candidate:${{ github.sha }}
          # Task 4: severity, ignore-unfixed, exit-code gate

      - name: Build and push
        id: build
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            # Task 5: ghcr.io/${{ github.repository }}:${{ github.sha }}
            # Task 5: ghcr.io/${{ github.repository }}:pr-${{ github.event.pull_request.number }} (PR runs only; use conditional if needed)

      - name: Comment digest on PR
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        env:
          DIGEST: ${{ steps.build.outputs.digest }}
        with:
          script: |
            // Task 6: post markdown comment with image, tag, digest, workflow link
