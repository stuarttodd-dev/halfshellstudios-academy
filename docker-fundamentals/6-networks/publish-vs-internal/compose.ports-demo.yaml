name: ch6-ports-demo

services:
  edge:
    image: nginx:stable-alpine
    ports:
      - "127.0.0.1:${HTTP_PORT:-8080}:80"

  probe:
    image: busybox:1.36
    command: ["sleep", "infinity"]
    depends_on:
      - cache

  cache:
    image: redis:7-alpine
